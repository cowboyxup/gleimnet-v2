{
  "name": "gleimnet-Frontend",
  "version": "1.0.0",
  "description": "Gleim.net is a social network that is used in the museum Gleimhaus. It is used for teaching media literacy for students.",
  "author": "",
  "license": "",
  "dependencies": {
    "jquery": "2.2.1",
    "ts-loader": "^0.8.1"
  },
  "scripts": {
    "clean": "rimraf node_modules doc typings && npm cache clean",
    "clean-install": "npm run clean && npm install",
    "watch": "webpack --watch --progress --profile --colors --display-error-details --display-cached",
    "build": "webpack --progress --profile --colors --display-error-details --display-cached",
    "build:prod": "webpack --progress --profile --colors --display-error-details --display-cached --optimize-dedupe",
    "typings": "typings",
    "postinstall": "typings install"
  },
  "dependencies": {
    "angular2": "2.0.0-beta.9",
    "angular2-jwt": "0.1.8",
    "es6-promise": "^3.0.2",
    "es6-shim": "^0.33.3",
    "reflect-metadata": "0.1.2",
    "rxjs": "5.0.0-beta.2",
    "zone.js": "0.5.15"
  },
  "devDependencies": {
    "typescript": "^1.8.7",
    "typings":"^0.7.5",
    "ts-loader":"0.8.1",
    "webpack": "1.12.14"
  }
}
